using System;
using System.Collections.Generic;
using System.IO;

namespace PedriniFabio
{
    public class Navigator : INavigator
    {
        /*
        private const string fileLeaderboard = "leaderboard.txt";

        public int GetLeaderboard()
        {
            int counter = 0;
            foreach (string line in File.ReadLines(fileLeaderboard))
            {
                System.Console.WriteLine(line);
                counter++;
            }
            return counter;
        }
        */
    }
}